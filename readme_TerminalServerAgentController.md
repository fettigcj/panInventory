# readme_TerminalServerAgentController — TS Agent Management

Purpose
- Interact with Palo Alto Networks Terminal Server (TS) Agents for tasks like status checks or configuration pushes related to User-ID.

Quick start
```
py .\TerminalServerAgentController.py --help
```

Scheduling tip
- Typically ad‑hoc; schedule only if you have periodic checks or sync actions to perform.

# readme_PanSysHealth_argParser — Argument Parser Variant for PanSysHealth

Purpose
- Companion/variant module focused on argument parsing and options for the PanSysHealth workflow.

Quick start
```
py .\PanSysHealth_argParser.py --help
```

Scheduling tip
- If used standalone, schedule like other scripts via Task Scheduler/cron with a wrapper.
